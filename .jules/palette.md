## 2024-05-22 - Accessibility in Media Grids
**Learning:** Icon-only buttons (like play, scroll-top) over media elements are a common accessibility blind spot.
**Action:** Systematically check all overlay buttons for `aria-label` and `role` attributes, especially in grid layouts.
