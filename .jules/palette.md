## 2024-05-22 - Card Clickability Patterns
**Learning:** Users expect the entire card (especially the image) to be clickable, not just the text. Small click targets frustrate users.
**Action:** When building cards with a single primary action, wrap the entire card content in a `Link` or button to maximize the hit area (Fitts's Law).
